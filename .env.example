# 数据库配置环境变量
# 复制此文件为 .env 并根据您的环境修改配置

# 数据库类型选择 (mysql 或 clickhouse)
DB_TYPE=mysql

# MySQL 配置
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password_here
MYSQL_DATABASE=stock

# ClickHouse 配置
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=9000
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=stock

# 使用说明:
# 1. 复制此文件为 .env
# 2. 修改上述配置为您的实际数据库连接信息
# 3. 在启动应用之前，确保环境变量已正确设置
# 4. 可以通过以下方式设置环境变量:
#    - 在 shell 中: export DB_TYPE=clickhouse
#    - 在 Python 中: os.environ['DB_TYPE'] = 'clickhouse'
#    - 使用 .env 文件并在代码中加载
